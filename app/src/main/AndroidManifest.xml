<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ==================== 权限声明 ==================== -->

    <!-- 录音权限：用于麦克风检测环境噪音 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- 通知权限：Android 13+ 需要运行时申请 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- 开机自启动权限 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 电池优化白名单（引导用户关闭电池优化） -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 检查电池优化状态 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 精确闹钟（Android 12+）-->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".AutoVolumeApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoVolume"
        tools:targetApi="35">

        <!-- 主 Activity：单 Activity 架构，使用 Compose Navigation -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AutoVolume">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            前台服务：负责后台运行噪音检测和音量调节。
            Android 14+ 要求声明 foregroundServiceType。
            "microphone" 类型表示需要麦克风访问。
        -->
        <service
            android:name=".service.AutoVolumeService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!--
            开机自启动广播接收器。
            监听 BOOT_COMPLETED 广播，在用户上次开启服务的情况下自动恢复服务。
        -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <!-- 部分厂商使用此 Action -->
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
